<?php
/**
 * @file
 * Bibliotek info module.
 */

/**
 * Implementats of hook_ctools_plugin_directory().
 */
function ding_bibliotek_info_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && $plugin == 'content_types') {
    return 'plugins/' . $plugin;
  }
}
